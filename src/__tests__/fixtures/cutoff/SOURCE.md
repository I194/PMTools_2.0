# fixtures/cutoff — SOURCE

Fixtures for `calculateCutoff` (Vandamme cutoff for VGP datasets).

## Fixtures

_Populated in Phase 1 Step 2._

## Citation rules

Each fixture cites one of:

- Vandamme 1994 paper (cutoff method, original definition)
- PmagPy equivalent function @ version `<X.Y.Z>`
