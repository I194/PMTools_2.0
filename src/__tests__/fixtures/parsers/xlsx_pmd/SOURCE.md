# fixtures/parsers/xlsx_pmd — SOURCE

Fixtures for `parserXLSX_PMD` (Excel workbook variant of PMD).

## Layout

- `real/` — real `.xlsx` files (Ivan provides during the phase).
- `synthetic/` — generated via the `xlsx` library: multi-sheet, empty sheet,
  formula cells, date cells, hidden columns, merged cells.

## Real-file origins

_Populated in Phase 1 Step 0.6._

## Synthetic-fixture matrix

_Populated in Phase 1 Step 4._
