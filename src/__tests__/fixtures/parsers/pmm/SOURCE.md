# fixtures/parsers/pmm — SOURCE

Fixtures for `parserPMM` (`.pmm` format).

## Layout

- `real/` — real `.pmm` files: `season1_north.pmm`, `season1_south.pmm`,
  `season2_extra.pmm`, `exampleDIR.pmm`, plus any others sourced from
  `test-data/` and `src/assets/examples/`.
- `synthetic/` — edge-case matrix.

## Real-file origins

_Populated in Phase 1 Step 0.6._

## Synthetic-fixture matrix

_Populated in Phase 1 Step 4._
