# fixtures/parsers/xlsx_dir — SOURCE

Fixtures for `parserXLSX_DIR` (Excel workbook variant of DIR).

## Layout

- `real/` — real `.xlsx` files.
- `synthetic/` — `xlsx`-lib-generated: multi-sheet, empty sheet, formula cells,
  date cells, hidden columns, merged cells.

## Real-file origins

_Populated in Phase 1 Step 0.6._

## Synthetic-fixture matrix

_Populated in Phase 1 Step 4._
