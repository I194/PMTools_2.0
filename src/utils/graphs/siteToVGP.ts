function siteToVGP(site: any) {

  // if ((site.sLat == 0) && (site.sLon == 0)) return;
  // var pole = new Site(site.sLat, site.sLon).poleFrom(site);
  // site.vgp = pole;

  // return pole;

}

export default siteToVGP;