# PMTools 2.0 — Agent Progress Log
# Each session appends entries below.
# Format: [DATE] — summary of changes
#
# Read this file at the start of every session to understand current state.
# Update it at the end of every session with what was accomplished.

## Active Tasks
- (none)

## Completed (cont.)
- [2026-03-28] Fix evaluation-report-3 bugs:
  - Bug 1 (major): Fixed remaining hooks order violations in OutputDataTablePMD, StatisticsDataTablePMD, DataTableDIR — same early-return pattern as Bug 4 above, replaced with conditional JSX rendering inside skeleton wrappers.
  - Bug 2 (major): Added safeParse helper in App.tsx for all localStorage JSON.parse calls — corrupted data no longer causes blank white screen; bad keys are removed and app loads normally.
  - Bug 3 (cosmetic): Guarded Zijderveld unit label computation against empty datasets — Math.max(...[]) no longer produces -INFINITY label.

## Completed
- [2026-03-28] Fix evaluation-report-2 bugs:
  - Bug 1 (critical): Added NaN validation in DIR/PMD parsers — rows with NaN in critical numeric fields (Dgeo/Igeo for DIR; x/y/z/mag/Dgeo/Igeo for PMD) are silently skipped. Non-critical NaN fields default to 0.
  - Bug 2 (minor): Added missing `graphics.lines.first` paragraph rendering in WhyPMToolsPage component.
  - Bug 3c: Added missing React list keys in ToolsPMD, ToolsDIR (Tooltip in .map), and Dot component (refactored from array-of-conditionals to individual conditional renders with keys).
  - Bug 3d: Configured Redux serializableCheck to ignore filesToData/sitesFileToLatLon async thunk actions (File objects are non-serializable).
  - Bug 4 (major): Stabilized component tree in MetaDataTablePMD and DataTablePMD — removed early returns that changed JSX structure, now always render skeleton wrapper with conditional DataGrid inside.
  - Skipped: Bug 5 (findDOMNode deprecation) — React 17 tolerates this, would need MUI component refactoring.
